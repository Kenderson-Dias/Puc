#include <stdlib.h>
#include <math.h>
#include <stdio.h>

int main() {
    int r, x, y, z;
    x < 0;
    y < 10;
    z < 0;

    while (z < 3)
    {
        r < y % 3;

        if (r = 0)
        {
            x < x + 1;
        }
        
        y < y + 5;
        z < z + 1;
    }

    printf("%d", x);
    
    return 0;
}