#include <stdio.h>
#include <stdlib.h>

/*2.d) Área de um círculo de raio igual a um*/

int main(){
    // Declaração e atribuição do valor de pi
    float pi = 3.14;
    // Declaração e atribuição do valor do raio
    float r = 1;
    // Declaração, atribuição e cálculo do valor da area do circulo
    float area = pow(pi * r, 2); 

    //Escrita na tela da area do circulo
}